# Backend deps for api_server.py, deployed as a Vercel Python function.
# Kept lightweight on purpose: Groq handles inference, so no torch/transformers
# needed here. See requirements-finetune.txt for the optional self-hosting path.
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
groq==0.11.0
python-dotenv==1.0.1
